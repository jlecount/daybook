import sys

def list_entries(book):
    # list-entries
    #   'max_entries' arg, optional
    #   'tags' arg, optional
    #   'text' arg, optional
    #   'after_date' arg, optional
    #   'before_date' arg, optional
    print("inside list_entries")
    sys.exit(0)

def list_tags(book):
    print("inside list_tags")
    sys.exit(0)

def create_entry(book):
    # create-entry
    #     all text is implicitly a body
    #     --tags  "a,b,c" are tags
    print("inside create_entry")
    sys.exit(0)
